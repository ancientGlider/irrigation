# План реализации меню настроек

Этот документ содержит план реализации модуля меню. Архитектура описана в [MENU_ARCHITECTURE.md](./MENU_ARCHITECTURE.md).

---

## Этап 8.1: Подготовка инфраструктуры

**Цель:** Создать файловую структуру и базовые определения.

| # | Задача | Файл | Зависимости |
|---|--------|------|-------------|
| 1 | Создать директорию `src/menu/` | — | — |
| 2 | Создать `menu_strings.h` — все строки меню (PROGMEM) | `menu_strings.h` | — |
| 3 | Создать `menu_data.h` — структуры MenuItem, Submenu, ParamDescriptor | `menu_data.h` | `menu_strings.h` |
| 4 | Заполнить таблицы MAIN_MENU_ITEMS, SUBMENUS, PARAM_DESCRIPTORS | `menu_data.h` | — |
| 5 | Добавить `getSoilRawValue(bool forceCheck)` в SystemController | `system_controller.h/cpp` | SoilSensor |
| 6 | Определить константы кнопок (ButtonIndex enum) | `menu.h` | — |

**Выходные данные:** Компилируемые заголовочные файлы с данными меню.

**Используемые существующие модули:**
- `Timer` — для автоповтора кнопок
- `Button` — API: `check()`, `getState()` (BUTTON_UNPRESSED / BUTTON_PRESSED / BUTTON_LONGPRESSED)
- `Settings::Manager` — `get(Key)`, `set(Key, value)`
- `SoilSensor` — `getRawSensorData(forceCheck)`

---

## Этап 8.2: Базовый API и FSM

**Цель:** Реализовать скелет Menu::Manager с FSM навигации.

| # | Задача | Файл | Зависимости |
|---|--------|------|-------------|
| 1 | Создать `menu.h` — объявление Menu::Manager | `menu.h` | `menu_data.h` |
| 2 | Реализовать `begin()`, `open()`, `close()`, `isOpen()` | `menu.cpp` | — |
| 3 | Реализовать FSM состояний (Closed, MainMenu, Submenu, Editing) | `menu.cpp` | — |
| 4 | Реализовать `update()` — маршрутизация по состояниям | `menu.cpp` | — |
| 5 | Реализовать навигацию по списку (up/down, scrollOffset) | `menu.cpp` | — |

**Выходные данные:** Работающая навигация без отрисовки (можно проверить через Serial).

---

## Этап 8.3: Отрисовка экранов

**Цель:** Реализовать визуализацию меню на дисплее.

| # | Задача | Файл | Зависимости |
|---|--------|------|-------------|
| 1 | Реализовать `_renderMainMenu()` — главное меню | `menu_render.cpp` | U8G2 |
| 2 | Реализовать `_renderSubmenu()` — подменю с параметрами | `menu_render.cpp` | — |
| 3 | Реализовать `_renderEditScreen()` — экран редактирования | `menu_render.cpp` | — |
| 4 | Добавить отрисовку live-значения для калибровки | `menu_render.cpp` | — |
| 5 | Реализовать форматирование значений (AsNumber, AsMinutes, AsPercent, AsEnum, AsTimeHHMM) | `menu_render.cpp` | — |
| 6 | Добавить отрисовку спецсимволов ("►", "*") по типу пункта | `menu_render.cpp` | — |

**Выходные данные:** Визуально корректное меню на дисплее.

---

## Этап 8.4: Редактирование параметров

**Цель:** Реализовать полный цикл изменения и сохранения настроек.

| # | Задача | Файл | Зависимости |
|---|--------|------|-------------|
| 1 | Реализовать `_changeValue()` с учётом step, min, max | `menu.cpp` | — |
| 2 | Реализовать динамические ограничения (TrainingMoisturePercent и др.) | `menu.cpp` | Settings |
| 3 | Реализовать `_saveValue()` — запись в Settings::Manager | `menu.cpp` | — |
| 4 | Реализовать отмену редактирования (CANCEL) | `menu.cpp` | — |
| 5 | Реализовать автоповтор: `BUTTON_LONGPRESSED` → Timer автоповтора | `menu.cpp` | Timer, Button API |
| 6 | Реализовать ускорение шага (×10) после 15 повторов | `menu.cpp` | — |

**Выходные данные:** Возможность изменять и сохранять все параметры.

---

## Этап 8.5: Action-команды

**Цель:** Реализовать выполнение действий (тренировка, очистка, сброс).

| # | Задача | Файл | Зависимости |
|---|--------|------|-------------|
| 1 | Реализовать `_executeAction()` — маршрутизация по ActionCode | `menu.cpp` | SystemController |
| 2 | Добавить вызов `SystemController::setTraining()` | `menu.cpp` | — |
| 3 | Добавить вызов `SystemController::setManualCleaning()` | `menu.cpp` | — |
| 4 | Реализовать диалог подтверждения для сброса настроек | `menu.cpp`, `menu_render.cpp` | — |
| 5 | Добавить вызов `Settings::resetToDefaults()` (если есть) | `menu.cpp` | Settings |

**Выходные данные:** Работающие Action-пункты меню.

---

## Этап 8.6: Интеграция и тестирование

**Цель:** Интегрировать меню в основной цикл и протестировать.

| # | Задача | Файл | Зависимости |
|---|--------|------|-------------|
| 1 | Интегрировать Menu::Manager в main.ino / loop() | `main.ino` | — |
| 2 | Проверить, что SystemController::process() работает при открытом меню | — | — |
| 3 | Протестировать все пункты меню и параметры | — | — |
| 4 | Проверить live-значение при калибровке датчика | — | SoilSensor |
| 5 | Измерить потребление RAM/Flash, сравнить с оценкой | — | — |
| 6 | Исправить найденные баги | — | — |

**Выходные данные:** Полностью работающее меню настроек.

---

## Зависимости между этапами

```
8.1 Подготовка инфраструктуры
 │
 ▼
8.2 Базовый API и FSM
 │
 ├──────────────────┐
 ▼                  ▼
8.3 Отрисовка      8.4 Редактирование
 │                  │
 └────────┬─────────┘
          ▼
        8.5 Action-команды
          │
          ▼
        8.6 Интеграция и тестирование
```

---

## Оценка трудозатрат

| Этап | Оценка | Примечание |
|------|--------|------------|
| 8.1 | 1-2 часа | Создание файлов и заполнение таблиц |
| 8.2 | 2-3 часа | FSM навигации, обработка кнопок |
| 8.3 | 3-4 часа | Отрисовка — самая объёмная часть |
| 8.4 | 2-3 часа | Логика редактирования |
| 8.5 | 1-2 часа | Action-команды простые |
| 8.6 | 2-3 часа | Тестирование и отладка |
| **Итого** | **11-17 часов** | — |

---

## Прогресс

- [x] Этап 8.1 — Создано: `menu_strings.h`, `menu_data.h`, добавлен `getSoilRawValue()` в SystemController
- [x] Этап 8.2 — Создано: `menu.h`, `menu.cpp` (FSM навигации, обработка кнопок)
- [x] Этап 8.3 — Создано: `menu_render.cpp` (отрисовка всех экранов)
- [x] Этап 8.4 — Реализовано в `menu.cpp` (changeValue, saveValue, динамические ограничения, автоповтор)
- [x] Этап 8.5 — Реализовано в `menu.cpp` (executeAction, диалог подтверждения)
- [x] Этап 8.6 — Создан `tests/menu/menu_test.ino` для интеграционного тестирования
